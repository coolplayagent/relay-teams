name: Publish To PyPI

on:
  push:
    tags:
      - "v*"

concurrency:
  group: publish-to-pypi-${{ github.ref }}
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    environment:
      name: pypi
      url: https://pypi.org/project/cool-play-agent-teams/
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Set up uv
        uses: astral-sh/setup-uv@v6

      - name: Install build dependencies
        run: uv sync --extra dev --no-default-groups

      - name: Derive release version from tag
        id: version
        shell: bash
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          if [[ -z "$VERSION" || "$VERSION" == "$GITHUB_REF_NAME" ]]; then
            echo "Expected tag format v<version>, got: $GITHUB_REF_NAME" >&2
            exit 1
          fi
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

      - name: Write release version file
        env:
          RELEASE_VERSION: ${{ steps.version.outputs.version }}
        run: |
          PYTHONPATH=src python -c "from pathlib import Path; import os; from agent_teams.release.versioning import write_version_file; write_version_file(os.environ['RELEASE_VERSION'], Path('src/agent_teams/_version.py'))"

      - name: Build distribution
        run: uv build

      - name: Publish distribution to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1

      - name: Report published version
        run: echo "Published version ${{ steps.version.outputs.version }}"
